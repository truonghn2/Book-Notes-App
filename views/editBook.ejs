<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Book</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <h1>Edit Book</h1>

  <!--Form to edit an existing book-->
  <form action="/edit/<%= book.id %>" method="POST">
    <label for="title">Title:</label>
    <input type="text" id="title" name="title" value="<%= book.title %>" required>
    <br>
    <label for="author">Author:</label>
    <input type="text" id="author" name="author" value="<%= book.author %>" required>
    <br>
    <label for="rating">Rating (out of 10):</label>
    <input type="number" id="rating" name="rating" value="<%= book.rating %>" min="0" max="10" required>
    <br>
    <label for="review">Review:</label>
    <textarea id="review" name="review"><%= book.review %></textarea>
    <br>
    <label for="read_date">Read Date:</label>
    <input type="date" id="read_date" name="read_date" value="<%= book.read_date %>" required>
    <br>
    <button type="submit">Update Book</button>
  </form>

  <!-- Link back to the main page -->
  <a href="/">Back to Home</a>
</body>
</html>